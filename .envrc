# Load environment variables from .kiro/settings/.env.local
# This file sources sensitive tokens that should not be committed
source_env .kiro/settings/.env.local

# Export for MCP servers
export GITHUB_PERSONAL_ACCESS_TOKEN
